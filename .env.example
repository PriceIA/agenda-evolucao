# Este projeto não tem build step nem le variaveis de ambiente em tempo de execucao.
# A URL e a anon key do Supabase estao hardcoded direto no index.html (por volta da linha 624).
#
# Este arquivo existe apenas como DOCUMENTACAO de quais valores sao esses, para o caso de uma
# futura migracao para um projeto com build (Vite/Next.js/etc) que passe a ler de .env.
#
# A anon key do Supabase e publica por design (protegida por RLS no banco, nao e um segredo).
# NUNCA coloque aqui a service_role key ou o PIN do Gestor.

SUPABASE_URL=https://indapxewhfobzxnwswhy.supabase.co
SUPABASE_ANON_KEY=
